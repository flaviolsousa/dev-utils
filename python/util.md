# Util

## Workon

```sh

mkvirtualenv <env-name>

which python3   #/usr/bin/python3
mkvirtualenv --python=/usr/bin/python3 <env-name>

workon <env-name>

pip install requests            # instalar a lib requests

pip freeze > requirements.txt   # backup da libs

pip install -r requirements.txt # instalar as libs em um txt
```
